<% if user_signed_in? %>
	Welcome <%= current_user.email %>!
<% end %>

<% if @posts.any? %>
	<% @posts.reverse.in_groups_of(3, false) do |posts_triplet| %>

	<section class="row">
		<%= render posts_triplet %>
	</section>

	<% end %>
<% else %>
	<h2>No posts yet</h2>
<% end %>